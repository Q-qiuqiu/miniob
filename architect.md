# miniob 项目架构设计

本文档详细描述 2025-miniob 项目的文件结构和架构设计。

## 项目整体结构

2025-miniob 是一个轻量级数据库系统，采用分层架构设计，包括 SQL 处理层、存储层和辅助工具组件。项目整体结构如下：

```
miniob/
├── src/                 # 源代码目录
├── test/                # 测试代码目录
├── unittest/            # 单元测试目录
├── benchmark/           # 性能测试目录
├── docs/                # 文档目录
├── etc/                 # 配置文件目录
├── deps/                # 第三方依赖目录
└── docker/              # Docker相关配置
```

## src 目录详细结构

### 核心目录概览

```
src/
├── common/              # 通用工具和基础组件
├── observer/            # 数据库服务器核心实现
├── oblsm/               # LSM树存储引擎实现
├── obclient/            # 客户端实现
├── memtracer/           # 内存跟踪工具（可选）
└── cpplings/            # C++学习示例代码（可选）
```

### 1. common/
通用工具和基础组件，提供跨模块使用的基础功能。

```
common/
├── conf/                # 配置相关。用于加载指定格式的配置文件。
├── io/                  # IO操作封装。提供跨平台的IO操作封装，主要是文件IO等。
├── lang/                # 语言工具类。OceanBase书中说OceanBase自己实现了很多标准库的功能，这个文件中的代码就是这些功能的实现
├── linereader/          # 行读取工具。主要用于交互式命令行界面中读取用户输入。
├── log/                 # 日志系统
├── math/                # 数学计算工具
├── mm/                  # 内存管理。提供内存分配和释放的封装，主要是内存池等。还有一些内存相关的工具类，比如内存泄漏检测工具。
├── os/                  # 操作系统封装。提供跨平台的操作系统相关功能封装，包括文件操作、目录操作、进程管理、信号控制等。path.h中list_file列出指定目录下符合正则表达式模式的所有文件，有一个TODO使用C++17的filesystem库替代（当前实现使用的是POSIX的目录遍历函数）（这个TODO不一定正确，是根据注释推测出来的，原有的TODO只是标注了，但是没有写具体内容）。
├── queue/               # 队列实现。仅包含一个简单的线程安全队列实现，用于在不同线程之间传递任务。使用RAII机制锁确保线程安全（增加优先队列和双端队列的实现）。
├── sys/                 # 系统运行码，rc文件
├── thread/              # 线程管理。提供线程相关的封装，主要是线程池等。
├── time/                # 时间相关工具。主要是日期时间处理模块和超时信息管理模块。
└── defs.h               # 公共定义。定义系统级的通用常量、错误码和宏。该文件包含了miniob项目中广泛使用的基础定义，包括状态码、跨平台线程ID获取、文件路径常量和浮点数比较精度等。
```

### 2. observer/
数据库服务器的核心实现，包含SQL处理、存储管理等核心功能。

```
observer/
├── catalog/             # 元数据管理
├── common/              # 服务器内部通用组件
├── event/               # 事件处理系统
├── net/                 # 网络通信模块
├── session/             # 会话管理
├── sql/                 # SQL处理层
├── storage/             # 存储层
└── main.cpp             # 服务器入口
```

#### 2.1 observer/sql/
SQL处理层，负责SQL语句的解析、优化和执行。

```
sql/
├── executor/            # SQL执行器
├── expr/                # 表达式计算
│   ├── aggregator       # 聚合函数实现。实现了SUM聚合函数，后续需要增加其他聚合函数的实现（TODO）。
│   ├── expression       # 表达式计算模块。定义了SQL查询中各种表达式的抽象基类和具体实现类，包括字段表达式、常量表达式、比较表达式、算术表达式、聚合表达式等。表达式是SQL查询计算的基本单元，用于处理数据值的获取、转换和计算。后续需要实现区分unbound和bound的表达式（TODO）。后续可以优化成在 `FieldExpr` 中存储 `chunk` 中某列的位置信息（TODO）。
├── operator/            # 操作符实现（逻辑和物理）
├── optimizer/           # 查询优化器
├── parser/              # SQL解析器（词法和语法分析）
│   ├── expression_binder # 表达式绑定上下文。维护查询中涉及的表信息，为表达式绑定提供必要的上下文环境。
│   ├── lex_sql          # SQL词法分析器。将SQL语句分解为词法单元（token），主要是根据SQL语法规则进行分析。目前逻辑运算只实现了AND，数据类型和SQL语句的支持都比较简单。后续可以根据需要增加其他逻辑运算和数据类型的支持（TODO）。
│   ├── parse_defs       # SQL解析器相关的数据结构、函数和接口定义。SELECT的解析进行了简化，仅支持from表，不能from表达式，且逻辑运算仅支持AND，也不支持复杂表达式（TODO）。INSERT语句也进行了简化，仅支持全字段插入，不能支持部分字段插入（TODO）。UPDATE语句也进行了简化，仅支持一个字段更新（TODO）。CREATE TABLE也进行了简化，后续需要补充（TODO）。创建索引仅支持对一个字段创建，后续也需要补充（TODO）。
│   ├── parse_stage      # 解析SQL语句。SQL解析阶段的核心处理函数，负责将SQL语句解析为结构化的SQL节点。
│   ├── parse            # SQL解析器的对外接口函数。主要是parse函数，用于将SQL语句文本解析为结构化的SQL节点对象。
│   ├── resolve_stage    # 解析SQL语句的解析阶段。ResolveStage负责将解析后的SQL语句（ParsedSqlNode）转换为可执行的语句对象（Stmt），同时进行语义检查、表和字段的绑定、权限验证等工作。
│   └── yacc_sql         # SQL语法分析器。将词法单元（token）序列转换为语法分析树（AST），主要是根据SQL语法规则进行分析。创建索引的语法也是仅支持一个字段，后续需要增加多个字段的支持（TODO）。
├── plan_cache/          # 执行计划缓存
│   └── plan_cache_stage # 执行计划缓存。尝试从Plan的缓存中获取Plan，如果没有命中，则执行Optimizer。目前什么都没有实现。后续可以参考OceanBase的实现原则来进行实现，优化效果比较好（TODO）。
├── query_cache/         # 查询结果缓存
│   └── query_cache_stage # 查询结果缓存。尝试从缓存中获取查询结果，如果没有命中，则执行查询。目前什么都没有实现。后续可以参考OceanBase的实现原则来进行实现（TODO）。
└── stmt/                # 语句定义和处理
```

#### 2.2 observer/storage/
存储层，负责数据的持久化和访问管理。

```
storage/
├── buffer/              # 缓冲池管理
│   ├── buffer_pool_log  # 缓冲池日志系统。主要是记录缓冲池的使用情况，比如分配、释放。
│   ├── disk_buffer_pool # 磁盘缓冲池。主要是管理磁盘上的数据页，包括读取、写入、缓存等。BufferPool实现在这个文件中。目前BufferPool实现未考虑优化，后续需要增加优化（TODO）。
│   ├── double_write_buffer # 双写缓冲区。这个逻辑需要研究，可以直接参考mysql的实现逻辑。主要是为了防止数据丢失，在写入数据到磁盘之前，先将数据写入双写缓冲区，然后再写入磁盘。将buffer中的数据刷入磁盘时，采用的策略是将buffer中的页全部写入磁盘，并且清空buffer。目前的解决方案是等buffer装满后再刷盘，可能会导致程序卡住一段时间。后续可以考虑使用异步刷盘策略，或者在刷盘时采用批量写入的方式（TODO）。VacuousDoubleWriteBuffer目前是空实现，不进行实际的双写操作，直接将页面写入磁盘。不确定后续需要不需要修改。DoubleWritePageKey为双写页面键结构，目前也是TODO状态。
│   ├── frame            # 数据页帧定义。页帧ID的hash函数是简单的位运算，不确定是否满足需求，后续需不需要使用标准哈希函数不确定。关于锁的设置目前使用的有点混乱，没有看懂，不过感觉后面可以使用更复杂的锁机制。
│   └── page             # 数据页和相关常量定义。主要是数据页的结构定义，包括日志序列号、校验和和数据部分。目前没有实现数据库页中常见的页尾倒排索引数据，不确定是不是不需要还是待实现。
├── clog/                # 事务日志系统
├── common/              # 存储通用组件
│   ├── chunk            # 数据块定义。数据块类，表示一组列的集合。有一个存储列ID的向量columnd_ids，需要移除它并支持多表，columnd_ids存储子操作符需要输出的ID（TODO）。目前没有看懂columnd_ids的作用。
│   ├── codec            # 有序编码解码工具。主要是将各种数据类型编码为有序字节序列，保持原始数据的大小顺序关系。这对于实现B+树等需要有序存储的结构非常有用。
│   ├── column           # 列类定义。列类，包含连续内存中的多个相同类型的值。当前仅支持定长类型，后续需要增加变长类型（TODO）。与列式存储的区别是，列式存储是将不同列的数据按列存储在不同的文件中，而当前的实现是将所有列的数据按行存储在一个连续的内存块中。主要是为了实现批量处理列数据的场景，比如在查询执行计划中，可能需要对某一列的数据进行过滤、排序、聚合等操作，使用 Column 类可以方便地管理这些中间数据。
│   ├── condition_filter # 条件过滤器定义。条件过滤器类，用于对记录进行条件过滤。当前支持单条件过滤和多条件过滤。但是多条件过滤采用的是与条件连接，后续可能需要增加或支持其他条件连接操作符（TODO）。且目前条件过滤只有两种，不确定后续是否需要增加其他条件过滤操作符（TODO）。这里没有实现不同类型的数据比较，比如整数跟浮点数之间的对比，但是需要实现（TODO）。
│   └── meta_util        # 元数据工具。主要是数据库元数据的管理，比如表索引文件的路径计算。
├── db/                  # 数据库管理
│   └── db               # 数据库类。一个DB实例负责管理一批表。当前DB的存储模式很简单，一个DB对应一个目录，所有的表和数据都放置在这个目录下。启动时，从指定的目录下加载所有的表和元数据。数据库对象没有做完整的并发控制。如果要完整的实现并发控制，需要实现表锁或类似的机制（TODO）。
├── default/             # 默认存储引擎处理器。
│   └── default_handler  # 默认存储引擎处理器。当前没有实现删除数据库和删除表的功能（TODO）。
├── field/               # 字段管理
│   ├── field_meta       # 字段元数据。主要是字段的结构定义，包括字段名、类型、长度等。
│   └── field            # 字段类。主要是字段的结构定义。
├── index/               # 索引管理。目前似乎是没有实现哈希索引，不确定后续是否需要实现。
│   ├── bplus_tree_index # B+树索引实现。主要调用bplus_tree中的BplusTreeHandler实现。关于索引扫描类的实现不是很明确，后续可能需要修改（TODO）。
│   ├── bplus_tree_log_entry # B+树索引日志操作类。包括了各种操作的类，提供了redo功能，非常复杂。redo主要调用bplus_tree中的BplusTreeHandler实现。
│   ├── bplus_tree_log   # B+树索引辅助实现。不仅实现了日志记录，还实现了事务辅助类和日志重做器。
│   ├── bplus_tree       # B+树实现。B+树的操作类型里没有更新，不知道是不是不需要还是说后续再实现（TODO）（B+树的更新操作比较特殊）。B+树的属性比较类中重载的括号运算符，仍需要优化（TODO）。还实现了属性比较器和键值比较器。B+树目前仅支持单个字段，后续需要支持多个字段（TODO）。聚簇索引（TODO）。实现非常复杂。
│   ├── index_meta       # 索引元数据。如果以后实现了多种类型的索引，还需要记录索引的类型，对应类型的一些元数据等（TODO）。只包含了索引的名字和对应字段的名字。
│   ├── index            # 索引虚基类。定义了索引类和索引扫描类。
│   ├── ivffat_index     # 向量索引。目前只进行了声明，没有实现（TODO）。
│   └── latch_memo       # latch备忘录。关于页帧的加锁和释放都是在这个类中实现的。
├── persist/             # 持久化相关。主要是文件的读写，PersistHandler在这个文件，负责打开、关闭、读取、写入文件。一个实例对应一个文件，可以使用create_file创建文件，open_file直接打开文件。但是如果文件已经创建，再次调用open_file打开其他文件会返回错误。关闭文件会重置文件描述符。
├── record/              # 记录管理
│   ├── heap_record_scanner # 堆表记录扫描器。主要是扫描堆表中的记录。对于一个纯粹的record遍历器来说，不应该关心表和事务（TODO）。
│   ├── lsm_record_scanner # LSM表记录扫描器。主要是扫描LSM表中的记录。
│   ├── record_log       # 记录日志类。
│   ├── record_manager   # 这里负责管理在一个文件上表记录(行)的组织/管理。表记录管理的内容包括如何在文件上存放、读取、检索。也就是记录的增删改查。可能存在优化空间，需要后续查看（TODO）。
│   ├── record_scanner   # 记录扫描器抽象基类。
│   └── record           # 记录类。主要是记录的结构定义，包括记录ID、主键、数据等。Record中有一个属性是key_，表示记录的主键，用于lsm-tree引擎，在后续版本中可能需要重构Record类以更好地支持这一功能（TODO）。
├── table/               # 表管理
│   ├── heap_table_engine # 堆表引擎。事务环境下插入、删除和更新都未实现（TODO）。（重要）
│   ├── lsm_table_engine  # LSM表引擎。基本都没有实现（TODO）。（重要）
│   ├── table_engine     # 表引擎接口。定义了表操作的核心接口，是表存储引擎的抽象表示。其他的表存储引擎需要实现这个接口。
│   ├── table_meta       # 表元数据类。主要是表的结构定义，包括表名、字段列表、索引列表、主键列表等。get_serial_size和to_string函数没有实现。
│   └── table            # 表类。主要是表的实现，包括创建、删除、插入、更新、查询等操作。TODO中有一个use TableEngine replace Table，但是没有看懂是要干啥。目前看到后面的代码，是在调用TableEngine的方法，所以不是很懂。里面主要函数的功能都是调用引擎实现的，只有少量函数是直接实现的。
└── trx/                 # 事务管理（必要时再研究，先保证基本功能）
│   ├── lsm_mvcc_trx
│   ├── mvcc_trx_log
│   ├── mvcc_trx
│   ├── trx
│   └── vacuous_trx
```

#### 2.3 observer/common/
服务器内部通用组件，提供跨模块使用的基础功能。

```
common/
├── type/                # 数据类型定义
│   ├── attr_type        # 属性类型定义。主要是枚举类型，用于表示字段的类型，添加新类型时需要修改这里。
│   ├── char_type        # 字符类型定义。类型转换和转换成本函数还没有实现。
│   ├── float_type       # 浮点数类型定义。在进行比较时，左边限制是浮点数，右边限制是整数或浮点数，为什么左边必须是浮点数，有待商议。在进行除法时，设置为浮点数最大值是不正确的。通常的做法是设置为NULL，但是当前的miniob没有NULL概念，所以这里设置为浮点数最大值，后续需要根据实际情况进行调整。没有定义类型转换和转换成本函数。
│   ├── integer_type     # 整数类型定义。在进行比较时，左边限制是整数，右边限制是整数或浮点数。没有实现类型转换，没有定义转换成本函数。
│   ├── vector_type      # 向量类型定义。仅进行了定义，没有任何实现。类型转换和转换成本函数都没有定义。
│   └── data_type        # 数据类型定义。主要是虚基类，其他类型都是其子类。使用时会根据类型进行动态绑定。
├── global_context       # 全局对象。目前没有任何实现。
├── init_setting         # 宏定义端口和参数。部分参数是字符串，可以后续自己调整成数值。
├── init                 # 初始化相关。
├── types                # 数据类型定义。主要是系统使用参数的类型别名定义以及枚举类型定义。
└── value                # 值管理。主要是值的封装类，用于表示字段的值。依赖关系为：attr_type -> data_type -> value。由于value中*_type是友元类，所以*_type可以访问value的成员。推荐使用value类来表示字段的值，而不是直接使用对应的type。实现非常详细，推荐阅读。
```

由于*_type是继承关系，所以实现的函数基本都一致，只是不同类型处理方法不一样。

### 3. oblsm/
LSM树存储引擎的实现，提供高性能的写入和范围查询能力。

```
oblsm/
├── benchmarks/          # LSM树性能测试
├── client/              # LSM树客户端
├── compaction/          # 压缩策略实现
├── include/             # 公共头文件
├── memtable/            # 内存表实现
├── table/               # 表和块管理
├── util/                # 工具类
└── wal/                 # 预写日志
```

### 4. obclient/
数据库客户端实现，提供与服务器交互的接口。

### 5. memtracer/
内存跟踪工具，用于监控和分析内存使用情况（可选组件）。

### 6. cpplings/
C++学习示例代码，包含各种C++特性的示例（可选组件）。

## 模块职责说明

### SQL处理层
- **parser**: 负责SQL语句的词法分析、语法分析，生成语法树
- **optimizer**: 对查询进行优化，生成高效的执行计划
- **executor**: 执行SQL语句，调用存储层接口完成数据操作
- **expr**: 提供表达式计算能力，支持各种操作符和函数
- **operator**: 实现各种逻辑和物理操作符，如扫描、连接、聚合等

### 存储层
- **db**: 数据库管理，包括创建、删除数据库等操作
- **table**: 表管理，包括表的创建、删除、查询等操作
- **record**: 记录管理，负责记录的插入、删除、修改、查询
- **index**: 索引管理，提供高效的数据检索能力
- **buffer**: 缓冲池管理，提高数据访问效率
- **trx**: 事务管理，提供ACID特性保证
- **clog**: 事务日志，确保数据持久性和可恢复性

### LSM树存储引擎
- **memtable**: 内存表实现，基于跳表结构
- **table**: SSTable和Block管理，负责数据的组织和存储
- **compaction**: 压缩策略实现，优化读性能和空间利用率
- **wal**: 预写日志，确保数据持久性
- **util**: 提供各种辅助工具，如布隆过滤器、LRU缓存等

# 锁说明

| 分类维度 | 锁类型 | 核心特点/目的 |
| :--- | :--- | :--- |
| **并发策略** | **悲观锁** | 先加锁，后访问。依赖数据库锁。 |
| | **乐观锁** | 先访问，提交时验版本。不依赖数据库锁。 |
| **锁粒度** | **行级锁** | 粒度最细，并发度高。 |
| | **页级锁** | 粒度中等。 |
| | **表级锁** | 粒度最粗，并发度低。 |
| | **数据库锁** | 粒度最大，用于管理。 |
| **兼容性** | **共享锁(S)** | 可并发读。 |
| | **排他锁(X)** | 独占，不可读写。 |
| | **更新锁(U)** | 预备更新，防止死锁。 |
| **意图** | **意向共享锁(IS)** | 表明意图在行上加S锁。 |
| | **意向排他锁(IX)** | 表明意图在行上加X锁。 |
| **特殊锁** | **间隙锁** | 锁范围，防幻读。 |
| | **临键锁** | 间隙锁+记录锁，InnoDB防幻读默认方式。 |
| | **自增锁** | 保证自增主键连续性。 |